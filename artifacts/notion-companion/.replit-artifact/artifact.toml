kind = "mobile"
previewPath = "/"
title = "Notion Companion"
version = "1.0.0"
id = "artifacts/notion-companion"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 21533

[services.development]
run = "pnpm --filter @workspace/notion-companion run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/notion-companion", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/notion-companion", "run", "serve" ]

[services.env]
PORT = "21533"
BASE_PATH = "/"
